# bdsai.vn — cổng dịch vụ local (Story 1.1 + 1.2)
# Copy thành `.env` để chạy local. `.env` KHÔNG commit (.gitignore).

# --- Web (Next.js) ---
NEXT_PUBLIC_API_BASE_URL=http://localhost:3101

# Supabase client phía FE (AD-2: FE read-only + auth; KHÔNG service-role key ở FE).
# Gateway kong port 54351 (dải 5435x). anon key là JWT demo local cố định (không nhạy cảm).
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54351
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0

# --- API (NestJS) ---
# Biến Supabase server-side (DATABASE_URL, SUPABASE_SERVICE_ROLE_KEY, pool...) xem apps/api/.env.example.
API_PORT=3101

# --- Sentry (Story 1.6 AC4) — error tracking (web + api) ---
# Optional — để trống (dev) = disabled. Prod: dán DSN từ sentry.io.
# Web client dùng NEXT_PUBLIC_SENTRY_DSN (public, safe). API dùng SENTRY_DSN (server).
# Xem apps/web/.env.example + apps/api/.env.example.
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
