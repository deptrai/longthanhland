# Story 1.1 — E2E Test (REAL DATA)

**Story:** 1-1-khoi-tao-monorepo-turborepo
**Tester:** opus-4.8 (skill bmad-qa-generate-e2e-tests)
**Ngày:** 2026-06-25
**Loại story:** Hạ tầng greenfield (monorepo scaffold + init Supabase local) — CHƯA có API/UI nghiệp vụ. Test = chạy thật stack + REAL HTTP healthcheck + REAL DB query. KHÔNG browser-test sâu (đúng phạm vi).
**Nguyên tắc:** 100% REAL — KHÔNG mock gì. Mọi assert dựa trên output thật của lệnh/HTTP/DB.

---

## VERDICT: ✅ PASS

6/6 test case REAL pass. `yarn dev` chạy đồng thời web (3100) + api (3101) không lỗi; REAL HTTP GET API trả đúng `{"status":"ok","service":"bdsai-api","defaultRole":"user"}`; web render homepage 200 (12.4KB HTML, có cả `Role` từ shared render ra runtime); Supabase stack lên đủ ở dải port 5435x; REAL DB query xác nhận `public` schema = 0 bảng nghiệp vụ; boundary `@bdsai/shared` được tiêu thụ thật ở cả 2 app. Đã teardown sạch (dev + supabase), không treo port, không đụng 2 project supabase khác.

---

## Môi trường (verify trước khi test)

| Mục | Giá trị | Nguồn |
|---|---|---|
| Docker daemon | 29.4.0 (chạy) | `docker info` |
| Supabase CLI | 2.90.0 | `supabase --version` |
| Node / yarn | v22.22.3 / 4.9.2 | `node -v` / `yarn -v` |
| Port 3100/3101 trước test | FREE | `lsof` |
| Port 5435x trước test | FREE | `lsof` |
| `yarn install --immutable` | OK (1 warning non-blocking: peer-dep webpack/ts-loader) | install log |

---

## Test cases

### TC1 — Supabase stack lên đủ + đúng dải port 5435x — REAL ✅ PASS

- **Type:** REAL (chạy `supabase start` + `supabase status` thật, query container thật).
- **Lưu ý môi trường:** `supabase start` (mặc định) FAIL vì container `supabase_studio_bdsai` báo **unhealthy** — đây là **lỗi MÔI TRƯỜNG, KHÔNG phải lỗi story**: log studio cho thấy Next.js của Studio **đã** "Ready in 1677ms" nhưng healthcheck timeout (máy đang chạy đồng thời 2 project supabase khác: chainlens-research + mmomarket → tài nguyên Docker căng). Studio KHÔNG cần cho test story này.
- **Cách xử lý:** chạy lại với `supabase start --ignore-health-check` → stack lên đủ. Đây vẫn là REAL stack, chỉ bỏ qua bước chờ healthcheck của studio.
- **Evidence (`supabase status`):**
  ```
  Studio  http://127.0.0.1:54353
  Mailpit http://127.0.0.1:54354
  API     http://127.0.0.1:54351
  DB      postgresql://postgres:postgres@127.0.0.1:54352/postgres
  ```
- **Port LISTEN thật (lsof):** 54351 ✅ / 54352 ✅ / 54353 ✅ / 54354 ✅ / 54357 (Analytics) ✅ — khớp dải 5435x.
- **Container health thật:** 11/12 healthy; chỉ `supabase_studio_bdsai` unhealthy (running, không ảnh hưởng API/DB/Auth/Storage).
- **Khớp story:** API 54351 / DB 54352 / Studio 54353 / Inbucket(Mailpit) 54354 / Analytics 54357 ✓.

### TC2 — `public` schema = 0 bảng nghiệp vụ — REAL ✅ PASS

- **Type:** REAL (psql thật trong container `supabase_db_bdsai`).
- **Lệnh:** `psql -U postgres -d postgres -c "SELECT ... FROM information_schema.tables WHERE table_schema='public'"`
- **Evidence:**
  ```
  table_count = 0
  (0 rows)
  ```
- **Khớp phạm vi:** đúng — Story 1.1 KHÔNG tạo bảng nghiệp vụ (việc đó thuộc Story 1.2).

### TC3 — `yarn dev` chạy đồng thời web + api không lỗi — REAL ✅ PASS (AC #4)

- **Type:** REAL (`yarn dev` thật qua turbo, 3 package: shared watch + api watch + web dev).
- **Evidence (dev log thật):**
  ```
  @bdsai/shared:dev: Found 0 errors. Watching for file changes.
  @bdsai/web:dev: ▲ Next.js 16.2.9 (Turbopack) - Local: http://localhost:3100  ✓ Ready in 892ms
  @bdsai/api:dev: [Nest] AppController {/}: Mapped {/, GET} route
  @bdsai/api:dev: [Nest] Nest application successfully started
  @bdsai/api:dev: [bdsai-api] listening on http://localhost:3101
  ```
- **Error scan dev log:** sạch (0 error/warn/exception).

### TC4 — REAL HTTP GET http://localhost:3101/ (API healthcheck) — REAL ✅ PASS (AC #1, #4)

- **Type:** REAL HTTP (curl tới NestJS server thật đang chạy — KHÔNG mock).
- **Evidence:**
  ```
  HTTP_STATUS=200
  BODY={"status":"ok","service":"bdsai-api","defaultRole":"user"}
  ```
- **Assert:** status 200 ✓; body khớp chính xác `{"status":"ok","service":"bdsai-api","defaultRole":"user"}` ✓.

### TC5 — REAL HTTP GET http://localhost:3100/ (web homepage SSR) — REAL ✅ PASS (AC #1, #4)

- **Type:** REAL HTTP (curl tới Next.js server thật — KHÔNG mock).
- **Evidence:**
  ```
  HTTP_STATUS=200
  BODY_BYTES=12441
  <!DOCTYPE html> ... <title>bdsai.vn</title>
  text: "bdsai.vn ... Sàn rao vặt bất động sản AI — Long Thành. Monorepo scaffold (Story 1.1). default role: user"
  ```
- **Assert:** status 200 ✓; HTML render thật (DOCTYPE + title + body 12.4KB) ✓.
- **Ghi chú browser:** story này chỉ là skeleton (không tương tác/feature), nên curl 200 + HTML hợp lệ là đủ — KHÔNG cần Chrome DevTools MCP browser-test sâu (đúng hướng dẫn).

### TC6 — Boundary check: `@bdsai/shared` được tiêu thụ thật ở cả web + api — REAL ✅ PASS (AC #1, #3)

- **Type:** REAL (đọc source 2 đầu boundary + chứng cứ runtime).
- **shared export (nguồn sự thật):** `packages/shared/src/types/index.ts:8` → `export type Role = 'user' | 'admin';`
- **WEB tiêu thụ:** `apps/web/app/(public)/page.tsx:1` → `import type { Role } from '@bdsai/shared';`
- **API tiêu thụ:** `apps/api/src/app.controller.ts:2` → `import type { Role } from '@bdsai/shared';`
- **Dependency khai báo:** cả 2 `package.json` đều có `"@bdsai/shared": "workspace:*"`.
- **Bằng chứng RUNTIME (mạnh nhất):** giá trị `Role` chảy ra cả 2 đầu lúc chạy thật — API body có `"defaultRole":"user"` và web HTML render `"default role: user"`. Shape khớp 2 phía, không lệch camelCase/snake_case (boundary OK).

---

## REAL vs MOCK

| Test | Loại | Ghi chú |
|---|---|---|
| TC1 Supabase stack + port | REAL | `supabase start --ignore-health-check` + `status` + lsof thật |
| TC2 public schema 0 bảng | REAL | psql thật trong db container |
| TC3 yarn dev đồng thời | REAL | turbo dev thật, dev log thật |
| TC4 API GET / | REAL HTTP | curl tới NestJS thật |
| TC5 web GET / | REAL HTTP | curl tới Next.js thật |
| TC6 boundary shared | REAL | source 2 đầu + runtime value |

**KHÔNG có mock nào.** Đúng kỳ vọng story hạ tầng 100% real.

---

## Phân biệt lỗi môi trường vs lỗi story

- **Lỗi môi trường (KHÔNG tính lỗi story):** `supabase start` mặc định fail do studio healthcheck timeout (máy chạy 2 project supabase khác → Docker căng tài nguyên). Studio Next.js thực tế đã Ready; bypass healthcheck → stack lên đủ. KHÔNG ảnh hưởng API/DB/Auth/Storage/Analytics — tức không ảnh hưởng phạm vi test của story.
- **Lỗi story:** KHÔNG có. Toàn bộ AC liên quan (build/dev/healthcheck/port/boundary/schema) đều PASS bằng chạy thật.
- **Ghi nhận cho story sau (không blocking):** (a) Supabase in nhắc bảo mật "tất cả service bind 0.0.0.0, API keys/JWT là default — không dùng production" → lưu ý cho Story 1.2 (kết nối Supabase) + story deploy. (b) CLI 2.90.0 có bản mới 2.107.0; story chốt 2.90.0 nên giữ nguyên, ghi nhận để nâng có chủ đích.

---

## Teardown (đã làm sạch)

- Kill `yarn dev` (turbo/next/nest) → port 3100/3101 = FREE ✓.
- `supabase stop` → bdsai containers gone, port 5435x = FREE ✓ (data backup trong docker volume, không xoá).
- 2 project supabase khác (chainlens-research, mmomarket) KHÔNG bị đụng ✓.
- Xoá temp file (/tmp/api-body.txt, web-body.html, bdsai-dev.log) ✓.

---

## Map AC → kết quả e2e

| AC | Kết quả | Test |
|---|---|---|
| #1 web Next16/React19 + api Nest11 + shared export type | ✅ PASS | TC3, TC4, TC5, TC6 |
| #2 turbo.json dev/build/lint/typecheck (đã verify ở review) | ✅ (re-confirm qua TC3 dev chạy thật) | TC3 |
| #3 naming kebab-case/PascalCase | ✅ PASS | TC6 (file `app.controller.ts`, type `Role`) |
| #4 yarn dev đồng thời web+api không lỗi | ✅ PASS | TC3, TC4, TC5 |

---

## Handoff

- **→ leader:** Story 1.1 **DONE** — e2e pass 100% với real data (REAL HTTP API + web, REAL DB query, REAL stack). sprint-status đã nâng `review` → `done`.
- **Cần Luis chốt (từ review, vẫn mở):** port Supabase chính thức là **5435x** (đã lệch khỏi 5434x trong 00-infra-decisions.md vì epsilon-local chiếm 5434x). E2e xác nhận 5435x chạy thật tốt — đề nghị chốt 5435x làm ràng buộc cứng cho các story sau tham chiếu nhất quán.
