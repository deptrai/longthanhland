# E2E Real-Data Test — Story 2.1: Đăng ký bằng email + phone

- **Story key:** `2-1-dang-ky-email-phone`
- **Agent:** e2e-tester (sonnet)
- **Ngày:** 2026-06-26
- **Verdict:** **PASS** (8/8 AC thỏa với real data) — 1 MEDIUM finding non-blocking (header link broken)

## Tóm tắt verdict

Story 2.1 verify bằng test THẬT: Supabase local (port 5435x) + Redis native (6379) +
Mailpit (54354) + psql + curl thật. Tất cả 8 AC thỏa với evidence real-data. 3 fix
review (HIGH-1 FK CASCADE, HIGH-2 enable_confirmations, MEDIUM-1 PII redact) đều
verify PASS bằng test thật. 1 finding MEDIUM mới phát hiện: header link `/auth/register`
→ 404 (route thực là `/register` do route group `(auth)`).

## Bảng test case (AC → verify thật)

| AC | Test case | Real/Mock | Pass/Fail | Evidence |
|----|-----------|-----------|-----------|----------|
| AC1 — Migration 0001 + RLS + FK CASCADE | psql `\d public_users` + RLS check + FK check + journal | REAL (psql Supabase DB 54352) | **PASS** | 8 cột đúng (id uuid PK, email, phone, phone_verified, role, banned, created_at, updated_at). `relrowsecurity=t`. 2 policy: `users_select_own` (SELECT, {0}), `service_role_all` (*, {16449}). FK `public_users_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id) ON DELETE CASCADE`. Journal 2 rows (0000+0001). |
| AC1 — RLS enforce (anon blocked) | curl REST anon SELECT + INSERT | REAL (Supabase REST 54351) | **PASS** | anon SELECT → `[]` (200, RLS chặn). anon INSERT → 401 `new row violates row-level security policy` (AD-2 anon không INSERT). |
| AC1 — RLS enforce (service-role full) | curl REST service-role SELECT | REAL | **PASS** | service-role SELECT → 2 rows (full access, bypass RLS). |
| AC1 — RLS enforce (auth own-row-only) | confirm user + signIn + curl REST with JWT | REAL | **PASS** | 2 rows total. Auth user 1 (JWT) SELECT → 1 row (chỉ row của mình, `id=auth.uid()` filter work). |
| AC1 — FK CASCADE (HIGH-1 fix) | psql DELETE auth.users → check public_users | REAL (psql) | **PASS** | userId fc199244...: before `count=1` → `DELETE FROM auth.users WHERE id=...` (DELETE 1) → after `count=0` (CASCADE xóa public_users row). |
| AC2 — Supabase Auth email + enable_confirmations | config.toml check + anon signUp + Mailpit | REAL | **PASS** | `config.toml:218 enable_confirmations=true` (section [auth.email]). anon signUp `mailpit-test-2-1@bdsai.vn` → response `confirmation_sent_at` populated. Mailpit `GET /api/v1/messages` → 1 email Subject "Confirm Your Email" To mailpit-test-2-1@bdsai.vn, Snippet "Confirm your email address ... enter the code: 960". |
| AC3 — Zod validation email | curl POST email sai `not-email` | REAL | **PASS** | 400 `{"statusCode":400,"message":"Validation failed","error":"Bad Request","details":[{"validation":"email","code":"invalid_string","message":"Email không hợp lệ","path":["email"]}]}` |
| AC3 — Zod validation phone | curl POST phone sai `123` | REAL | **PASS** | 400 `details:[{"validation":"regex","code":"invalid_string","message":"Số điện thoại không hợp lệ (phải là số VN 10-11 chữ số)","path":["phone"]}]` |
| AC3 — Zod validation password | curl POST password ngắn `Ab1!` | REAL | **PASS** | 400 `details:[{"code":"too_small","minimum":8,"type":"string","message":"Mật khẩu tối thiểu 8 ký tự","path":["password"]}]` |
| AC4 — POST /auth/register | curl POST hợp lệ + psql row | REAL | **PASS** | curl `e2e-2-1@bdsai.vn` phone `0902000001` → 201 `{"userId":"c7a3c7cd-...","email":"e2e-2-1@bdsai.vn","phoneVerified":false,"emailVerified":false}`. psql: row tồn tại id=c7a3c7cd-..., email=e2e-2-1@bdsai.vn, phone=0902000001, phone_verified=f, role=user, banned=f. Queue job enqueued (api log `jobId:13 queue:bdsai-email`). |
| AC5 — Phone normalization +84→0 | curl POST phone `+84987654399` + psql | REAL | **PASS** | curl 201. psql: `phone=0987654399` (normalized từ +84987654399). |
| AC6 — Web form /register | curl GET /register + grep HTML | REAL (Next dev 3100) | **PASS** | `/register` → 200. HTML có `<form>`, `<input type=email>`, "Số điện thoại", `<input type=password>` (password + confirmPassword), "Đăng ký". Next API proxy `/api/auth/register` → forward NestJS (201/409 work). |
| AC6 — Header link (FINDING MEDIUM) | curl GET /auth/register | REAL | **FAIL** | `/auth/register` → 404 "This page could not be found." Header `site-header.tsx:52` link `href="/auth/register"` nhưng route thực là `/register` (route group `(auth)` không add path). Build route map confirm: `/register` (Static), `/api/auth/register` (Dynamic) — KHÔNG có `/auth/register` page. |
| AC7 — Trùng email → 409 | curl POST cùng email | REAL | **PASS** | curl `e2e-2-1@bdsai.vn` (lần 2) → 409 `{"statusCode":409,"message":"Email đã được đăng ký","error":"Conflict"}`. |
| AC7 — Trùng phone → 409 | curl POST cùng phone | REAL | **PASS** | curl cùng phone → 409 `{"statusCode":409,"message":"Số điện thoại đã được sử dụng","error":"Conflict"}` (Supabase Auth enforce phone unique). |
| AC8 — PII redact rawMsg (MEDIUM-1 fix) | trigger dup email + grep api log | REAL (pino log) | **PASS** | dup-email log: `rawMsg: "A user with this email address has already been registered"` — KHÔNG có email raw (regex redact sẽ catch nếu Supabase message chứa email). success log: `userId, action:register, reason:success` — không email/phone raw. dup-phone log: `reason:duplicate-phone` — không phone raw. |
| AC8 — Không phá 1.1-1.6 (build/lint/typecheck/test) | yarn build/lint/typecheck/test | REAL | **PASS** | build 3/3 successful, lint 4/4, typecheck 4/4, test 59 api (8 suites) + 10 web (3 suites) = 69 PASS. |
| AC8 — E2e test suite | yarn test:e2e (per-suite isolation) | REAL | **PASS** | 24/24 tests pass khi chạy per-suite. auth.e2e-spec.ts PASS (Story 2.1). queue/sentry/health/vietnamese-fts/app PASS. |

## Boundary-crossing check result

| Boundary | Claim code | Test thật | Match? |
|----------|-----------|-----------|--------|
| Supabase Auth createUser tạo user thật? | admin.createUser service-role | curl register → 201 userId + psql auth.users có row (verify qua DELETE CASCADE) | **YES** |
| public_users row thật sự insert? | Drizzle insert service-role | psql SELECT → row tồn tại với đúng email/phone/role | **YES** |
| FK CASCADE thật sự work? | migration 0001 ALTER TABLE ADD CONSTRAINT | psql DELETE auth.users → public_users count 1→0 | **YES** |
| Mailpit thật sự nhận email? | enable_confirmations=true → GoTrue gửi | curl anon signUp → Mailpit /api/v1/messages → 1 email "Confirm Your Email" | **YES** |
| PII redact thật sự redact? | safeRawMsg regex replace | pino log rawMsg không chứa email raw | **YES** |
| RLS thật sự enforce? | policy users_select_own + service_role_all | anon SELECT [] + INSERT 401, service-role 2 rows, auth JWT 1 row (own) | **YES** |
| Phone normalize thật sự lưu 0xxx? | normalizePhone +84→0 | psql phone=0987654399 (từ +84987654399) | **YES** |
| Zod validation thật sự gate? | registerSchema email/phone/password | curl 3 case sai → 400 với details đúng | **YES** |

## Phân biệt lỗi môi trường vs lỗi story

| Lỗi | Loại | Xử lý |
|------|------|-------|
| Supabase bdsai chưa start khi bắt đầu | Môi trường | `supabase start --ignore-health-check` → OK |
| Redis native (PID 35049) trên 6379 — không do story start | Môi trường (pre-existing) | Dùng native redis (PONG OK), KHÔNG start docker bdsai-redis (tránh conflict). Dev log cũng note để nguyên native redis. |
| Phone 0901234567 / 0987654321 đã tồn tại (leftover data dev) | Môi trường (data drift từ dev test trước) | Dùng phone fresh (0902000001, 0987654399, 0902000055, 0902000066) |
| e2e suite sentry/queue FAIL khi chạy all-together (parallel + --runInBand) | Môi trường (BullMQ worker cleanup flake — Redis "Connection is closed" giữa các suite) | Chạy per-suite isolation → 24/24 PASS. auth.e2e-spec.ts (Story 2.1) PASS nhất quán. KHÔNG phải regression Story 2.1. |
| API dev server bị kill khi shell exit (nohup) | Môi trường | Dùng background exec tool → stable |
| Header link `/auth/register` → 404 | **LỖI STORY** (MEDIUM) | site-header.tsx:52 link sai — route thực `/register` (route group `(auth)`). Form work nhưng nav broken. |

## Note fix HIGH-1 / HIGH-2 / MEDIUM-1 — verify trạng thái sau fix

### HIGH-1 — FK ON DELETE CASCADE → FIXED + VERIFY PASS
- Migration `0001_brainy_frightful_four.sql` cuối file có `ALTER TABLE public_users ADD CONSTRAINT public_users_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id) ON DELETE CASCADE`.
- psql `pg_get_constraintdef` → `FOREIGN KEY (id) REFERENCES auth.users(id) ON DELETE CASCADE`.
- Real test: DELETE auth.users → public_users 1→0 (CASCADE). **PASS.**

### HIGH-2 — enable_confirmations=true → FIXED + VERIFY PASS
- `config.toml:218` (section [auth.email]) `enable_confirmations = true`.
- anon signUp → `confirmation_sent_at` populated + Mailpit nhận email "Confirm Your Email" (OTP code 960). **PASS.**
- **Nuance:** `admin.createUser` (path register API) KHÔNG tự gửi confirmation email kể cả khi `enable_confirmations=true` (chỉ anon signUp path gửi). Email verify thực sự cho path admin.createUser deferred sang Story 2.2 (inviteUserByEmail) hoặc Story 6.2 (EmailProcessor custom SMTP). Comment auth.service.ts đã sửa bỏ claim sai. Queue files (queue.service.ts, email.processor.ts) vẫn còn claim "Supabase Auth đã gửi email tự động" — non-blocking LOW follow-up.

### MEDIUM-1 — rawMsg duplicate-email log redact PII → FIXED + VERIFY PASS
- `auth.service.ts:70-81` redact email/phone regex trước khi log rawMsg.
- pino log dup-email: `rawMsg: "A user with this email address has already been registered"` — không có email raw. **PASS.**

## Note nuance: admin.createUser path không tự gửi email (deferred Story 2.2)

- Register API dùng `supabase.auth.admin.createUser` (service-role) → tạo user `email_confirmed=false` nhưng KHÔNG gửi confirmation email (GoTrue admin path bypass mailer).
- Email verify path đúng = anon signUp (GoTrue mailer gửi) HOẶC `admin.inviteUserByEmail` (Story 2.2) HOẶC EmailProcessor custom SMTP (Story 6.2).
- Verify Mailpit qua anon signUp path → PASS (email nhận).
- Story 2.1 chấp nhận no-email cho register API path (deferred), AC2 thỏa qua config + anon signUp proof. Comment code đã sửa bỏ claim sai.

## E2e test suite — chi tiết

- `yarn test:e2e` (parallel): 24 tests passed, 1 suite (sentry) failed-to-run (Redis Connection closed flake).
- `--runInBand`: 22 passed, 2 failed (queue suite Redis flake).
- **Per-suite isolation:** tất cả 6 suite PASS (24/24 tests):
  - `auth.e2e-spec.ts` PASS (Story 2.1: AC4 register, AC4 public_users row, AC7 dup email, AC3 email/phone/password, AC5 normalize)
  - `queue.e2e-spec.ts` PASS (2 tests)
  - `sentry.e2e-spec.ts` PASS (3 tests)
  - `health.e2e-spec.ts` PASS (2 tests)
  - `vietnamese-fts.e2e-spec.ts` PASS (5 tests)
  - `app.e2e-spec.ts` PASS (1 test)
- Cross-suite failure = BullMQ worker cleanup timing (Redis connection closed giữa suite) — **môi trường flake, KHÔNG regression Story 2.1.**

## Finding mới (MEDIUM — non-blocking)

### MEDIUM-NEW — Header link `/auth/register` broken → 404
- **File:** `bdsai/apps/web/components/shared/site-header.tsx:52` — `<Link href="/auth/register">Đăng ký</Link>`
- **Mô tả:** Route page thực là `/register` (file `app/(auth)/register/page.tsx` — route group `(auth)` không add path). Build route map confirm: `/register` (Static), KHÔNG có `/auth/register` page. Click "Đăng ký" trong header → 404.
- **Cách sửa:** Đổi `href="/auth/register"` → `href="/register"` trong site-header.tsx:52.
- **Severity:** MEDIUM — form work tại /register, Next API proxy work, nhưng user không reach form qua header nav (UX break).
- **Không block:** AC6 form本身 PASS (render OK + proxy OK). Nav link là polish bug.

## Teardown confirmation

- `supabase stop` → Stopped supabase local development setup (port 54351/54352/54353/54354 free).
- Kill API dev server (nest start --watch) → port 3101 free.
- Kill web dev server (next dev -p 3100) → port 3100 free.
- Redis native (PID 35049, port 6379) — pre-existing, KHÔNG do story start, để nguyên (per dev log note). Không có docker bdsai-redis container (dùng native redis).
- `lsof -i :3100 -i :3101 -i :54351 -i :54352 -i :54354 -s` → rỗng (không LISTEN).
- Temp files cleaned (`/tmp/bdsai-api-2-1.log`, `/tmp/register-page.html`).

## Kết luận

**PASS.** Story 2.1 thỏa 8/8 AC với real-data evidence (Supabase Auth + DB + Mailpit + Redis thật, không mock). 3 fix review (HIGH-1 FK CASCADE, HIGH-2 enable_confirmations, MEDIUM-1 PII redact) đều verify PASS bằng test thật. 1 finding MEDIUM mới (header link `/auth/register` → 404, nên là `/register`) — non-blocking, recommend fix trong story này hoặc story sau (polish nav). Core auth functionality (register, validation, phone normalize, duplicate detect, RLS, FK CASCADE, email verify, PII redact) all work thật.

**Recommendation cho leader:** Story 2.1 DONE. Fix MEDIUM-NEW (header link `/auth/register`→`/register`) có thể gộp vào story này (1 dòng) hoặc defer sang story UI sau.
