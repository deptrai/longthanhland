# E2E Real-Data Test Report — Story 1.2: Kết nối Supabase

- **Story key:** 1-2-ket-noi-supabase (Epic 1 — HẠ TẦNG)
- **Agent:** e2e-tester (sonnet, skill bmad-qa-generate-e2e-tests)
- **Ngày:** 2026-06-25
- **Verdict:** **PASS** — 7/7 AC verify thật PASS

---

## 1. Phương pháp verify (REAL DATA — không tin log dev)

Story là hạ tầng (kết nối Supabase, chưa có UI/API nghiệp vụ) → test cách:
1. `supabase start --ignore-health-check` thật (dải port 5435x).
2. `yarn db:migrate` chạy thật → psql query thật.
3. `yarn workspace @bdsai/api build` thật → `node dist/main.js` chạy api thật port 3101.
4. `curl http://localhost:3101/health/supabase` real HTTP → assert 200 + body.
5. `docker exec supabase_db_bdsai psql` query thật verify AC1/AC4/AC6.
6. `yarn test` (unit) + `yarn test:e2e` (integration với Supabase đang chạy) thật.
7. Đọc code `health.controller.ts`/`health.service.ts`/`database.module.ts` so shape với response thật (boundary-crossing).

KHÔNG dùng mock cho những gì test thật được. Mock chỉ ở nhánh "DB down" của e2e (thay DRIZZLE bằng stub throw — đúng cách, không thể stop DB thật vì phá test khác).

---

## 2. Bảng test case AC → verify thật

| AC | Test case | Real/Mock | Pass/Fail | Evidence (response/output thật) |
|----|-----------|-----------|-----------|----------------------------------|
| **AC1** | `yarn db:migrate` chạy thật + psql `__drizzle_migrations` tồn tại + chạy lại idempotent | REAL | **PASS** | `migrate` lần 2 → NOTICE `relation "__drizzle_migrations" already exists, skipping` + `[✓] migrations applied successfully!` (không lỗi). psql: `drizzle \| __drizzle_migrations` tồn tại. |
| **AC2** | `yarn workspace @bdsai/api test` — env.validation.spec (Zod fail-fast, thiếu biến → throw, AD-8 không lộ secret) | REAL | **PASS** | `Test Suites: 2 passed, 2 total / Tests: 11 passed, 11 total`. env.validation.spec 7 ca pass. |
| **AC3** | `node dist/main.js` + `curl /health/supabase` real HTTP | REAL | **PASS** | `HTTP=200 time=0.240s` (cold), warm `0.230s`. Body: `{"status":"ok","db":{"status":"up"},"auth":{"status":"up"},"storage":{"status":"up"}}`. Root `/` → 200 `{"status":"ok","service":"bdsai-api","defaultRole":"user"}`. |
| **AC4** | psql `SELECT tablename FROM pg_tables WHERE schemaname='public'` sau migrate | REAL | **PASS** | Public schema rỗng (0 bảng). Check 6 bảng nghiệp vụ (`public_users`,`public_listings`,`inquiries`,`cross_posts`,`ai_results`,`imported_listings`) → rỗng. Chỉ có `drizzle.__drizzle_migrations` (metadata). |
| **AC5** | Đọc code `database.module.ts` verify postgres.js `prepare:false` + pool config | REAL (code read) | **PASS** | `database.module.ts:34-38` `postgres(url,{prepare:false,max,idle_timeout})`; `max` từ `DB_POOL_MAX` (default 10), `idle_timeout` từ `DB_IDLE_TIMEOUT` (default 20) qua ConfigService. `OnModuleDestroy` `client.end({timeout:5})` đóng pool. `.env.example` ghi rõ prod Supavisor 6543 + prepare:false. |
| **AC6** | `docker exec supabase_db_bdsai psql -c "SELECT version()"` + đọc `config.toml` | REAL | **PASS** | `PostgreSQL 15.8 on aarch64-unknown-linux-gnu`. `supabase/config.toml:37` `major_version = 15`. Khớp stack doc (PG15). |
| **AC7** | `yarn test` (unit 11) + `yarn test:e2e` (health.e2e-spec với Supabase chạy) | REAL | **PASS** | Unit: 11 passed. E2e: `Test Suites: 2 passed / Tests: 3 passed` — `health.e2e-spec.ts` (200 khi up + 503 error shape khi DB down + redact url) + `app.e2e-spec.ts`. `jest.setTimeout(120_000)` hấp thụ cold start. |

**Tổng: 7/7 AC PASS.**

---

## 3. Boundary-crossing check result

So sánh response HTTP thật với code:

| Trường | Response thật (curl) | Code (`health.service.ts`/`health.controller.ts`) | Khớp? |
|--------|----------------------|---------------------------------------------------|-------|
| `status` | `"ok"` | `allUp ? 'ok' : 'degraded'` (line 48-49) | ✓ |
| `db.status` | `"up"` | `checkDb()` → `SELECT 1` → `{status:'up'}` (line 52-55) | ✓ |
| `auth.status` | `"up"` | `checkAuth()` → `listUsers({page:1,perPage:1})` no error → `{status:'up'}` (line 62-69) | ✓ |
| `storage.status` | `"up"` | `checkStorage()` → `listBuckets()` no error → `{status:'up'}` (line 76-82) | ✓ |
| HTTP code (all up) | `200` | controller return result trực tiếp (line 41) → 200 | ✓ |
| Error shape (down) | (e2e verify) 503 `{statusCode,message,error,details}` | controller throw `HttpException({statusCode:503,message,error:'Service Unavailable',details:{db,auth,storage}},503)` (line 30-38) | ✓ (e2e assert pass) |
| AD-8 redact | response không chứa `postgres://` | `safeMessage` redact `postgres(ql)://` → `[redacted-url]` (line 90-94); e2e assert `not.toMatch(/postgres(ql)?:\/\//)` | ✓ |

**Kết luận boundary:** Shape response thật KHỚP hoàn toàn với code. Không có bug ranh giới. Promise.all 3 check độc lập (try/catch riêng) → một thành phần down không crash endpoint (AC3).

---

## 4. Phân biệt lỗi môi trường vs lỗi story

| Hiện tượng | Loại | Xử lý |
|------------|------|-------|
| `supabase start` cần `--ignore-health-check` (studio health timeout do máy chạy nhiều project Supabase) | **Môi trường** (không phải lỗi story) | Dùng `--ignore-health-check` — đã biết từ Story 1.1. |
| Lần cold-start trước (dev log 73s) do Docker quá tải | **Môi trường** | Lần này Supabase đã warm → cold curl 0.24s, warm 0.23s. Test đặt `jest.setTimeout(120_000)` hấp thụ. |
| Process `node dist/main.js` bị kill khi shell command trả về (dùng `&`/`nohup`/`disown` không giữ được) | **Môi trường tooling** (exec shell kill process group) | Dùng exec tool `run_in_background:true` để giữ server sống lúc curl. Không liên quan code story. |
| Supabase CLI v2.90 hiển thị thêm key mới `sb_publishable`/`sb_secret` (S3) nhưng `status -o env` vẫn trả JWT `ANON_KEY`/`SERVICE_ROLE_KEY` demo cố định | **Môi trường** | `.env` dùng JWT demo key — khớp `supabase status -o env` (verify diff: chỉ khác quote, value identical). Auth/Storage healthcheck pass → key hoạt động. |

**Không có lỗi story tồn đọng.** 6 finding low từ review (LOW-1..LOW-6) đều non-blocking, không ảnh hưởng AC.

---

## 5. Teardown confirmation

```
$ pkill -f "dist/main.js"        → port 3101 free
$ supabase stop                  → Stopped supabase local development setup (data backed up to docker volume)
$ lsof -i :3101 -i :54351 -i :54352
(empty — all ports free)
$ rm -f /tmp/health*.json /tmp/bdsai-*.log /tmp/supabase_keys.txt /tmp/env_keys.txt
(done — temp files removed)
```

Port 3101, 54351, 54352 đều free. Docker volume `bdsai` backup data (lần start sau khôi phục).

---

## 6. Kết luận

**Verdict: PASS — Story 1.2 DONE.**

7/7 AC verify thật PASS với dữ liệu thật (Supabase PG15.8 chạy, Drizzle migrate idempotent, healthcheck HTTP 200 body đúng shape, 0 bảng nghiệp vụ, 14 test pass). Boundary-crossing check khớp hoàn toàn. AD-1/AD-2/AD-5/AD-8 tuân. Teardown sạch.

**Evidence chính:**
- AC3: `curl http://localhost:3101/health/supabase` → `HTTP=200` `{"status":"ok","db":{"status":"up"},"auth":{"status":"up"},"storage":{"status":"up"}}` (warm 230ms)
- AC6: `PostgreSQL 15.8` + `config.toml major_version=15`
- AC1: `drizzle.__drizzle_migrations` tồn tại, migrate idempotent
- AC4: `pg_tables WHERE schemaname='public'` = rỗng
- AC7: unit 11 passed + e2e 3 passed

**Recommendation cho story sau (2.1/3.1):** xử lý 6 finding low từ review (LOW-1 main.ts port qua process.env, LOW-2 dead token, LOW-3 export PG_CLIENT thừa, LOW-4 redact nhánh non-throw, LOW-5 document journal rỗng, LOW-6 idle_timeout positive) khi chạm lại file — không chặn Done story này.
