# E2E Real-Data Test Report — Story 1.5: Vietnamese Full-Text Search setup

- **Story key:** `1-5-vietnamese-full-text-search`
- **Agent:** e2e-tester (sonnet)
- **Ngày:** 2026-06-26
- **Verdict:** **PASS** (8/8 AC, 12/12 test case, real-data verify qua Supabase local + psql + e2e jest)
- **Baseline commit:** `307a4a4959`

## Tóm tắt verdict

Story 1.5 (hạ tầng DB — Vietnamese FTS) PASS toàn bộ. Supabase local PG 15.8 start thật (port 5435x), migration 0000 apply thành công qua Drizzle, 2 extension (unaccent + pg_trgm) enabled thật, 2 function IMMUTABLE thật (provolatile='i'), FTS match 3/3 case true (long thanh→Long Thành, can ho→Căn hộ, dat nen→Đất nền), pg_trgm similarity=1 + operator %=true, idempotent (migrate lần 2 exit 0, count=1, 0 dup), 0 bảng nghiệp vụ public, e2e jest 12/12 pass, CI gate (build/lint/typecheck/test) PASS. Teardown sạch (port free).

## Bảng test case (AC → verify thật)

| AC | Test case | Real/Mock | Pass/Fail | Evidence (psql output thật) |
|----|-----------|-----------|-----------|-----------------------------|
| AC1 | Extension unaccent + pg_trgm enabled | REAL (psql pg_extension) | PASS | `pg_trgm 1.6`, `unaccent 1.1` (2 rows) |
| AC2 | f_unaccent_to_tsvector + f_unaccent_query IMMUTABLE | REAL (psql pg_proc provolatile) | PASS | `f_unaccent_to_tsvector \| i`, `f_unaccent_query \| i` (schema=public) |
| AC2 | tsvector bỏ dấu + simple config | REAL (psql SELECT function) | PASS | `f_unaccent_to_tsvector('Long Thành đất nền căn hộ')` = `'can':5 'dat':3 'ho':6 'long':1 'nen':4 'thanh':2` (6 token không dấu) |
| AC2 | tsquery plain text → AND | REAL (psql SELECT function) | PASS | `f_unaccent_query('long thanh')` = `'long' & 'thanh'` (plainto_tsquery auto-AND) |
| AC3 | FTS match "long thanh"→"Long Thành" | REAL (psql @@ operator) | PASS | `m1 = t` |
| AC3 | FTS match "can ho"→"Căn hộ" | REAL (psql @@ operator) | PASS | `m2 = t` |
| AC3 | FTS match "dat nen"→"Đất nền" | REAL (psql @@ operator) | PASS | `m3 = t` |
| AC4 | pg_trgm similarity > 0 | REAL (psql similarity()) | PASS | `sim = 1` (sau unaccent+lower cả 2 = "long thanh" → identical) |
| AC4 | pg_trgm operator % match | REAL (psql % operator) | PASS | `trgm_match = t` (threshold 0.3) |
| AC5 | Migration idempotent (migrate lần 2 exit 0) | REAL (yarn db:migrate + psql count) | PASS | `[✓] migrations applied successfully!` exit 0; `migration_count = 1`; 0 duplicate hash |
| AC6 | 0 bảng nghiệp vụ schema public | REAL (psql pg_tables) | PASS | `(0 rows)` — chỉ extension + function, không table |
| AC6 | schema/index.ts vẫn export {} | REAL (read file) | PASS | `export {};` (rỗng, comment ghi bảng ở story sau) |
| AC7 | Code comment Deferred ES + ranking + simple config | REAL (read migration SQL) | PASS | Comment dòng 6, 29-36: "AC7 — Giới hạn ranking từ ghép (xem ARCHITECTURE-SPINE.md Deferred dòng 330)", "simple config tách Long Thành thành 2 token riêng", "ES migrate là DEFERRED — trigger khi user complaint", TODO Story 3.3 GIN index |
| AC8 | e2e integration test (yarn test:e2e) | REAL (jest + DB thật) | PASS | `Test Suites: 3 passed, 3 total` / `Tests: 12 passed, 12 total` (app + health + vietnamese-fts) |
| AC6/AC8 | CI gate không phá 1.1-1.4 (build/lint/typecheck/test) | REAL (yarn turborepo) | PASS | build 3/3, lint 4/4, typecheck 4/4, test web 10 + api 11 — all exit 0 |

**Tổng: 12/12 test case PASS (8/8 AC).**

## Evidence chính (psql output thật)

### 1. Extension (AC1)
```
 extname  | extversion
----------+------------
 pg_trgm  | 1.6
 unaccent | 1.1
(2 rows)
```

### 2. Function IMMUTABLE (AC2)
```
        proname         | provolatile | schema
------------------------+-------------+--------
 f_unaccent_to_tsvector | i           | public
 f_unaccent_query       | i           | public
(2 rows)
```
→ Cả 2 `provolatile='i'` (IMMUTABLE) — safe cho GIN index (Story 3.3). Schema = `public` (KHÔNG phải `drizzle`).

### 3. tsvector + tsquery (AC2)
```
f_unaccent_to_tsvector('Long Thành đất nền căn hộ')
= 'can':5 'dat':3 'ho':6 'long':1 'nen':4 'thanh':2

f_unaccent_query('long thanh')
= 'long' & 'thanh'
```
→ Bỏ dấu + lowercase + simple config (không stem). plainto_tsquery auto-AND plain text.

### 4. FTS match (AC3)
```
 m1 | m2 | m3
----+----+----
 t  | t  | t
(1 row)
```
→ "long thanh"→"Long Thành" (t), "can ho"→"Căn hộ" (t), "dat nen"→"Đất nền" (t).

### 5. pg_trgm similarity + % operator (AC4)
```
similarity(unaccent(lower('Long Thành')), unaccent(lower('long thanh'))) = 1
'long thanh' % 'Long Thành' = t
```
→ similarity = 1 (> 0), operator % = true (threshold 0.3). Lưu ý: similarity = 1 vì sau unaccent+lower cả 2 chuỗi identical ("long thanh" = "long thanh"). Operator % trên raw string (có dấu vs không dấu) vẫn match qua trigram overlap.

### 6. Idempotent (AC5)
```
yarn db:migrate (lần 2) → [✓] migrations applied successfully! (exit 0)
SELECT count(*) FROM drizzle.__drizzle_migrations → migration_count = 1
SELECT hash, count(*) ... HAVING count(*) > 1 → (0 rows)
```
→ Không duplicate, Drizzle journal track đúng.

### 7. 0 bảng nghiệp vụ (AC6)
```
SELECT tablename FROM pg_tables WHERE schemaname='public' → (0 rows)
```

### 8. e2e test (AC8)
```
PASS  test/app.e2e-spec.ts
PASS  test/vietnamese-fts.e2e-spec.ts
PASS  test/health.e2e-spec.ts
Test Suites: 3 passed, 3 total
Tests:       12 passed, 12 total
Time:        3.916 s
```

### 9. CI gate (AC6 — không phá 1.1-1.4)
```
yarn build      → Tasks: 3 successful, 3 total (exit 0)
yarn lint       → Tasks: 4 successful, 4 total (exit 0)
yarn typecheck  → Tasks: 4 successful, 4 total (exit 0)
yarn test       → web 10 passed + api 11 passed (exit 0)
```

## Boundary-crossing check (SQL code vs DB thật)

| Điểm check | Code SQL (migration file) | DB thật (psql verify) | Khớp? |
|------------|---------------------------|----------------------|-------|
| Extension CREATE | `CREATE EXTENSION IF NOT EXISTS unaccent/pg_trgm` | pg_extension trả 2 rows (pg_trgm 1.6, unaccent 1.1) | KHỚP |
| Function IMMUTABLE | `LANGUAGE sql IMMUTABLE` (dòng 43, 64) | `provolatile='i'` cho cả 2 function | KHỚP |
| f_unaccent_to_tsvector logic | `to_tsvector('simple', unaccent(lower(input)))` | tsvector = `'can':5 'dat':3 'ho':6 'long':1 'nen':4 'thanh':2` (bỏ dấu + simple) | KHỚP |
| f_unaccent_query logic | `plainto_tsquery('simple', unaccent(lower(input)))` (KHÔNG phải to_tsquery như story gốc — dev resolve) | tsquery = `'long' & 'thanh'` (plain text auto-AND) | KHỚP |
| FTS match hoạt động | `@@` operator giữa tsquery + tsvector | m1/m2/m3 = t (3/3 match) | KHỚP |
| pg_trgm similarity | `similarity()` + `%` operator | sim=1, trgm_match=t | KHỚP |
| Idempotent | `CREATE EXTENSION IF NOT EXISTS` + `CREATE OR REPLACE FUNCTION` | migrate lần 2 exit 0, count=1, 0 dup | KHỚP |
| 0 bảng nghiệp vụ | Migration chỉ extension + function (không CREATE TABLE) | pg_tables public = 0 rows | KHỚP |

**Kết luận boundary-crossing:** Code SQL migration KHỚP 100% với DB thật. Không có gap giữa ý định code và hành vi thật. Function thật sự IMMUTABLE, FTS thật sự match, extension thật sự enabled.

## Phân biệt lỗi môi trường vs lỗi story

- **Lỗi môi trường:** KHÔNG có. Supabase start OK (--ignore-health-check không cần thiết lần này — health check pass), extension available trên PG15 contrib, Drizzle `--custom` hoạt động.
- **Lỗi story:** KHÔNG có (trong phiên bản hiện tại). Dev log ghi 3 lỗi story đã fix trong dev: (1) to_tsquery syntax error → plainto_tsquery, (2) TEMP table không persist qua postgres.js → regular table + try/finally, (3) TypeScript null-check → helper firstRow(). Tất cả đã resolve trước khi đến trạm e2e — verify lại không tái diễn.
- **Note nhỏ (non-blocking):** `schema/index.ts` comment vẫn ghi "File migration 0000 đầu tiên sẽ sinh khi Story 2.1 thêm bảng public_users" — thực tế Story 1.5 đã tạo migration 0000 qua `--custom`. Đây là doc comment stale (không ảnh hưởng AC), không phải lỗi story. Đề xuất story sau (2.1) update comment.

## Teardown confirmation

- `cd bdsai && supabase stop` → "Stopped supabase local development setup." exit 0. Data backed up to docker volume (`com.supabase.cli.project=bdsai`) — giữ cho story sau.
- Port check: `lsof -i :54351 -i :54352` → rỗng (exit 1, free). ✓
- File tạm: `rm -f /tmp/fts-*.log /tmp/bdsai-*.json` → done. ✓
- Test table `fts_test`: e2e test DROP trong finally (public tables = 0 sau test). ✓
- Migration 0000 đã apply trên DB local volume (giữ cho story sau — Story 3.1 sẽ thêm migration 0001).

## Kết luận

**Story 1.5 PASS — 8/8 AC, 12/12 test case, real-data verify.**

Vietnamese FTS hạ tầng DB sẵn sàng cho Story 3.3 (search service sẽ inject DRIZZLE + gọi `f_unaccent_to_tsvector`/`f_unaccent_query` + tạo GIN index trên `public_listings`). ES migrate DEFERRED (trigger = user complaint về relevance, tham chiếu ARCHITECTURE-SPINE.md dòng 330). Story done.
