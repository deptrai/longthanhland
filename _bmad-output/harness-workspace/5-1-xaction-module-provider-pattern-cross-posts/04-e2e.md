# E2e Test Report — Story 5.1: XactionModule + Provider Pattern + cross_posts

## Verdict: PASS (12/12 tests pass — REAL DATA)

## Environment
- Redis: PONG (local)
- Supabase local: connected (port 54351 API / 54352 DB)
- API server: running (pid 9286, nest start --watch)
- XACTION_ENABLED_PROVIDERS: facebook,cho_tot,zalo

## Tests

### T1 — AC2: Migration apply thật — PASS
- 9 columns đúng spec (id, listing_id, platform, external_url, provider_job_id, status, error_message, created_at, updated_at)
- Enum cross_post_platform: 3 giá trị (facebook, cho_tot, zalo)
- Enum cross_post_status: 5 giá trị (pending, posted, failed, removed, removal_failed)
- 3 indexes: cross_posts_pkey, idx_cross_posts_listing, idx_cross_posts_listing_platform (unique)
- Idempotent: chạy 2 lần → "already exists, skipping" + DO blocks, không lỗi

### T2 — AC7: POST /xaction/promote (stub provider) — PASS
- Seller register + email verify + login → JWT
- Listing DRAFT → submit → PENDING → admin approve → PUBLISHED
- POST /xaction/promote {listingId, platforms:["facebook"]}
- Response: HTTP 202, crossPosts[1] status=pending, enqueued=1, errors=[]

### T3 — AC9: Promote non-PUBLISHED → 400 — PASS
- Listing DRAFT (không submit)
- POST /xaction/promote → HTTP 400 "Chỉ quảng bá được tin đang hiển thị (PUBLISHED)"

### T4 — AC9/E1: Duplicate platform → 409 trong errors[] — PASS
- Listing đã promote FB (T2)
- Promote lại FB → HTTP 202, crossPosts=[], errors[{platform:"facebook", message:"Tin đã quảng bá trên nền tảng này"}]

### T5 — AC9/E6: Ownership → 403 — PASS
- Seller B promote listing của seller A → HTTP 403 "Không có quyền quảng bá tin này"

### T6 — AC1/E10: Stub provider post thành công — PASS
- Sau T2 + 3s đợi processor
- DB: status='posted', external_url='https://stub.example/post/...', provider_job_id='stub-...'

### T7 — AC7: GET /xaction/cross-posts — PASS
- GET /xaction/cross-posts?listingId=X → HTTP 200, array 1 cross-post (status=posted)

### T8 — H1 fix: GET cross-posts non-owner → 403 — PASS
- Seller B GET cross-posts của listing seller A → HTTP 403 "Không có quyền xem cross-post của tin này"

### T9 — AC7: DELETE /xaction/cross-posts/:id — PASS
- DELETE cross-post → HTTP 202 "Cross-post removal enqueued"
- Sau 3s → DB status='removed' (stub provider removePost resolve void)

### T10 — AC5/AD-9: Listing SOLD → cross-post removal — PASS
- Listing PUBLISHED + 2 cross-posts (facebook + zalo, cả 2 posted)
- POST /marketplace/listings/:id/sold → status SOLD
- Sau 3s → cả 2 cross-posts status='removed' (AD-9 lifecycle removal enqueued + processed)

### T11 — AC5/AD-9/H3: Listing DELETE → cross-post removal before cascade — PASS
- Listing PUBLISHED + 1 cross-post (cho_tot, posted)
- DELETE /marketplace/listings/:id → {success:true}
- Sau 3s → cross_posts count=0 (cascade delete), public_listings count=0
- Removal job chạy BEFORE cascade delete (stub provider removePost called với job data)

### T12 — AC8/E5: Invalid platform → 400 — PASS
- POST /xaction/promote {platforms:["tiktok"]} → HTTP 400 "Dữ liệu không hợp lệ" (Zod enum validation)
- Note: disabled-but-valid platform (VD zalo khi chỉ enable facebook) cần restart API — covered by unit test provider-registry.spec.ts

## Real data verification
- Redis: PONG ✅
- Supabase: connected ✅ (DB + Auth + Storage up)
- Migration 0013: applied + idempotent ✅
- BullMQ queue 'cross-post': processor chạy thật (status pending→posted trong 3s) ✅
- Stub provider: postListing + removePost gọi thật ✅
- AD-9 lifecycle: SOLD + DELETE trigger removal thật ✅

## Issues found
- Không có issue mới. Tất cả 12 test pass với real data.
- T12: test "disabled but valid platform" cần restart API với config khác — skipped vì unit test đã cover logic (provider-registry.spec.ts test enabled/disabled).

## Teardown
- cross_posts test rows: deleted
- public_listings test rows: deleted
- Supabase auth users test: deleted
- public_users test rows: deleted
- Cleanup complete, không để lại data rác.

## Conclusion
Story 5.1 PASS e2e real-data verification. Foundation (IPromotionProvider + cross_posts table + XactionModule + BullMQ queue + AD-9 lifecycle + API endpoints + ownership/IDOR guards) hoạt động đúng spec với Redis + Supabase thật.
