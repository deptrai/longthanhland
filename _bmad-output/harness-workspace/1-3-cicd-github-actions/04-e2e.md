# E2E Real-Data Test Report — Story 1.3: CI/CD GitHub Actions → Vercel + Dokploy

| Field | Value |
|---|---|
| Story key | `1-3-cicd-github-actions` |
| Agent | e2e-tester (sonnet) |
| Ngày | 2026-06-26 |
| Verdict | **PASS** (6/7 AC PASS, AC4 PARTIAL — M1 /listings gate broken, fix sau) |
| Baseline | `7b923aeab9` (production, sau Story 1.2) |
| Loại story | HẠ TẦNG CI/CD — test local: workflow syntax + Docker build thật + smoke test container + yarn build/test thật |

## 1. Môi trường test

| Tool | Version | Ghi chú |
|---|---|---|
| Node.js | v22.22.3 | |
| yarn | 4.9.2 (corepack) | |
| Docker | 29.4.0 | Docker Desktop macOS |
| Supabase CLI | 2.90.0 | `supabase start --ignore-health-check` |
| actionlint | 1.7.12 | **Cài mới trong e2e** (`brew install actionlint`) — dev log + review log ghi chưa cài, e2e cài + chạy PASS |
| OS | macOS (arm64) | Docker Desktop `--network host` = Docker VM network, KHÔNG phải Mac host |

## 2. Bảng test case — AC → verify thật

| AC | Test case | Real/Mock | Pass/Fail/Partial | Evidence |
|---|---|---|---|---|
| **AC1** CI workflow lint+typecheck+build+test cả 3 package, trigger production+develop | Đọc `ci.yml` + actionlint + yarn build/lint/typecheck/test thật | REAL | **PASS** | `ci.yml`: trigger `push`+`pull_request` `branches: [production, develop]`; `working-directory: bdsai`; `yarn install --immutable` → `yarn build` → `yarn lint` → `yarn typecheck` → `yarn test`. actionlint exit 0. yarn build 3/3, lint 4/4, typecheck 4/4, test 11/11 PASS. |
| **AC2** Deploy trigger production CHỈ, web→Vercel CLI, api→Docker+GHCR+Dokploy | Đọc `deploy.yml` + actionlint | REAL (config) | **PASS** | `deploy.yml`: `on: push: branches: [production]` CHỈ. Job `deploy-web`: `npx vercel@latest deploy --prod --yes --token ${{ secrets.VERCEL_TOKEN }} --cwd apps/web` + env `VERCEL_ORG_ID`/`PROJECT_ID`. Job `deploy-api`: `docker/build-push-action@v6` push `ghcr.io/<owner>/bdsai-api:{latest,sha}` + `curl -X POST $DOKPLOY_WEBHOOK_URL -H "Authorization: Bearer $DOKPLOY_TOKEN"`. KHÔNG chạy deploy thật (out of scope local — cần GitHub UI). |
| **AC3** Gate — deploy needs ci-gate, fail CI → không deploy | Đọc `deploy.yml` verify `needs: [ci-gate]` | REAL (config) | **PASS** | `deploy-web` line 59: `needs: ci-gate`. `deploy-api` line 98: `needs: ci-gate`. `ci-gate` job chạy lại full lint+typecheck+build+test. Fail bất kỳ → deploy job không chạy. actionlint exit 0. |
| **AC4** Lighthouse CI, SEO ≥ 90, post-deploy gate | Đọc `.lighthouserc.js` + verify web app routes | REAL (config) | **PARTIAL** | `.lighthouserc.js`: `assert.assertions['categories:seo']: ['error', { minScore: 0.9 }]` (error = fail job). Job `lighthouse needs: deploy-web` + `lighthouse-fail-issue if: failure()`. **NHƯNG** URL `https://bdsai.vn/listings` KHÔNG tồn tại — web app chỉ có route `/` (build output: `Route (app) ┌ ○ / └ ○ /_not-found`). Gate `/listings` sẽ 404 → spurious fail. **M1 confirmed.** Gate `/` OK. KHÔNG chạy Lighthouse thật (cần web deploy production — out of scope local). |
| **AC5** Secrets qua GitHub Secrets, không hardcode | grep workflow + Dockerfile + docker history | REAL | **PASS** | `grep -rE "(eyJ\|sk-\|supabase_service_role\|postgres://postgres:postgres)" ci.yml deploy.yml Dockerfile` → exit 1 (CLEAN, no match). `docker history --no-trunc \| grep -iE "DATABASE_URL=\|SERVICE_ROLE_KEY=\|SUPABASE_URL=\|eyJhbGci\|sk-"` → exit 1 (CLEAN, no secret baked). Tất cả secret qua `${{ secrets.* }}` trong workflow. Dockerfile chỉ `ENV NODE_ENV` + `ENV API_PORT` (không nhạy cảm). |
| **AC6** Dockerfile multi-stage, node:22-alpine, EXPOSE 3101, HEALTHCHECK, build local OK | docker build thật + smoke test container 200 + đọc Dockerfile | REAL | **PASS** | Docker build SUCCESS (19/19 FINISHED, exit 0). Multi-stage: `FROM node:22-alpine AS builder` → `FROM node:22-alpine AS runner`. `EXPOSE 3101`. `HEALTHCHECK --interval=30s --timeout=5s --start-period=20s --retries=3 CMD wget -qO- http://localhost:3101/health/supabase \|\| exit 1`. `CMD ["node", "dist/main.js"]`. Smoke test: container boot OK, `docker exec wget /health/supabase` → **HTTP 200** `{"status":"ok","db":{"status":"up"},"auth":{"status":"up"},"storage":{"status":"up"}}`. Docker HEALTHCHECK status = `healthy`. Image 333MB (< 400MB soft target). **M3 confirmed** (builder `yarn build` build cả web, không chỉ api+shared — optimization, fix sau). |
| **AC7** Workflow syntax hợp lệ + Dockerfile build + yarn PASS | actionlint + docker build + yarn build/lint/typecheck/test | REAL | **PASS** | actionlint 1.7.12 exit 0 trên cả `ci.yml` + `deploy.yml` (KHÔNG syntax error). `yarn workflow:validate` → "✔ actionlint PASS". Docker build SUCCESS. yarn build 3/3, lint 4/4, typecheck 4/4, test 11/11 PASS. |

### Tóm tắt: 6 PASS + 1 PARTIAL = **PASS** (AC4 PARTIAL do M1, non-blocking)

## 3. Evidence chi tiết — test thật đã chạy

### 3.1 Workflow syntax validate (actionlint 1.7.12)
```
$ actionlint .github/workflows/ci.yml .github/workflows/deploy.yml
EXIT=0  (no output = no errors)

$ yarn workflow:validate
→ Dùng actionlint để validate GitHub Actions syntax.
✔ actionlint PASS cho cả ci.yml + deploy.yml.
EXIT=0
```
→ **PASS**. actionlint 1.7.12 (cài mới trong e2e — dev log + review log ghi chưa cài, fallback PyYAML). Bắt được GitHub Actions-specific syntax (không chỉ YAML parse).

### 3.2 Docker build thật
```
$ docker build -f bdsai/apps/api/Dockerfile -t bdsai-api:test bdsai/
[+] Building ... (19/19) FINISHED
 => [builder 5/6] RUN yarn install --immutable          CACHED
 => [builder 6/6] RUN yarn build                        CACHED
 => [runner 7/10] RUN yarn workspaces focus @bdsai/api --production  CACHED
 => [runner 8/10] COPY --from=builder .../packages/shared/dist
 => [runner 9/10] COPY --from=builder .../apps/api/dist
 => naming to docker.io/library/bdsai-api:test
EXIT=0
```
→ **BUILD SUCCESS**. Multi-stage hoạt động, layers cached từ dev build.

### 3.3 Docker smoke test thật (container + real Supabase)
```
$ supabase start --ignore-health-check
→ Supabase local running (API 54351, DB 54352, Studio 54353)

$ docker run -d --name bdsai-smoke --network host --env-file bdsai/apps/api/.env bdsai-api:test
→ Container ID 547a2fea...

$ docker logs bdsai-smoke (sau 12s)
[Nest] 1  LOG [NestFactory] Starting Nest application...
[Nest] 1  LOG [SupabaseService] Supabase client khởi tạo (host=127.0.0.1:54351, bucket=listings)
[Nest] 1  LOG [RoutesResolver] HealthController {/health}
[Nest] 1  LOG [RouterExplorer] Mapped {/health/supabase, GET} route
[Nest] 1  LOG [NestApplication] Nest application successfully started
[bdsai-api] listening on http://localhost:3101

$ docker exec bdsai-smoke wget -qO- http://localhost:3101/health/supabase
{"status":"ok","db":{"status":"up"},"auth":{"status":"up"},"storage":{"status":"up"}}
EXIT=0

$ docker inspect --format='{{.State.Health.Status}}' bdsai-smoke
healthy

$ docker inspect --format='{{range .State.Health.Log}}{{.ExitCode}} {{.Output}}{{end}}' bdsai-smoke
0 {"status":"ok","db":{"status":"up"},"auth":{"status":"up"},"storage":{"status":"up"}}
```
→ **SMOKE TEST PASS**. Container boot OK, NestJS khởi động, endpoint `/health/supabase` trả **HTTP 200** với `db:up, auth:up, storage:up` — REAL DATA, container gọi Supabase thật qua `--network host`. Docker HEALTHCHECK status = `healthy` (exit 0).

**Lưu ý macOS:** `curl` từ Mac terminal đến `localhost:3101` trả HTTP_CODE=000 (connection refused) — do Docker Desktop macOS `--network host` chia sẻ Docker VM network, KHÔNG phải Mac host network. Container bên trong vẫn hoạt động đúng (verified qua `docker exec`). Đây là **lỗi môi trường macOS**, KHÔNG phải lỗi story.

### 3.4 yarn build/lint/typecheck/test thật
```
$ yarn build
 Tasks:    3 successful, 3 total (FULL TURBO)
$ yarn lint
 Tasks:    4 successful, 4 total (FULL TURBO)
$ yarn typecheck
 Tasks:    4 successful, 4 total (FULL TURBO)
$ yarn test
 @bdsai/api:test:  Test Suites: 2 passed, 2 total
 @bdsai/api:test:  Tests:       11 passed, 11 total
 Tasks:    3 successful, 3 total
EXIT=0
```
→ **PASS** (không phá Story 1.1/1.2).

### 3.5 Secret audit thật (grep)
```
$ grep -rE "(eyJ|sk-|supabase_service_role|postgres://postgres:postgres)" \
    .github/workflows/ci.yml .github/workflows/deploy.yml bdsai/apps/api/Dockerfile
GREP_EXIT=1  (no match = CLEAN)
```
→ **CLEAN**. Workflow + Dockerfile KHÔNG chứa literal secret.

### 3.6 Docker layer audit thật (docker history)
```
$ docker history bdsai-api:test --no-trunc | grep -iE "DATABASE_URL=|SERVICE_ROLE_KEY=|SUPABASE_URL=|SUPABASE_ANON_KEY=|postgres://postgres:postgres|eyJhbGci|sk-"
SECRET_BAKE_EXIT=1  (no match = CLEAN)
```
→ **CLEAN**. Không có secret bake vào image layer (AD-8 OK).

**Lưu ý:** `docker history | grep -iE "SECRET|KEY|TOKEN"` (pattern rộng) match `KEY` trong node:22-alpine base image layer (GPG key verification cho node binary download) — đây là standard node image layer, KHÔNG phải secret. Grep cụ thể secret pattern → exit 1 (clean).

### 3.7 Image size
```
$ docker images bdsai-api:test --format "{{.Size}}"
333MB
```
→ 333MB < 400MB soft target. Hơi trên ngưỡng "lý tưởng" 300MB nhưng chấp nhận được (NestJS + drizzle + postgres + supabase-js prod deps trên node:22-alpine ~170MB base). **M3** (builder build cả web) góp phần waste layer size — fix sau.

## 4. Boundary-crossing check

| Check | Dockerfile khai báo | Container thật | Match? |
|---|---|---|---|
| EXPOSE port | `EXPOSE 3101` (line 52) | App binds `:::3101` (0.0.0.0:3101, verified `ss -tlnp` inside container) | ✅ MATCH |
| HEALTHCHECK endpoint | `wget -qO- http://localhost:3101/health/supabase` (line 57) | Docker HEALTHCHECK status = `healthy`, log exit 0 + body `{"status":"ok",...}` | ✅ MATCH |
| CMD | `CMD ["node", "dist/main.js"]` (line 59) | Container chạy `node dist/main.js`, NestJS boot OK | ✅ MATCH |
| Env qua ConfigService | Dockerfile `ENV API_PORT=3101` (default) | App đọc port qua `ConfigService.get('API_PORT')` (main.ts:16), env override qua `--env-file` | ✅ MATCH |
| Secret runtime (AD-8) | Dockerfile KHÔNG `ENV` secret | Container nhận secret qua `--env-file .env` (runtime), `docker history` clean | ✅ MATCH |
| Health endpoint logic | `GET /health/supabase` → 200 nếu all up, 503 nếu any down (health.controller.ts) | Container trả 200 `{"status":"ok","db":"up","auth":"up","storage":"up"}` | ✅ MATCH |

→ **Boundary-crossing check PASS**. Dockerfile khai báo khớp với thực tế container runtime.

## 5. Phân biệt lỗi môi trường vs lỗi story

| Hiện tượng | Phân loại | Lý do |
|---|---|---|
| `curl` từ Mac → `localhost:3101` trả HTTP_CODE=000 | **Lỗi môi trường** (macOS Docker Desktop) | `--network host` trên macOS = Docker VM network, không phải Mac host. Container bên trong vẫn hoạt động (verified `docker exec`). Linux thì `--network host` = host network, curl sẽ 200. |
| Supabase CLI 2.90.0 key format mới (sb_publishable/sb_secret) vs .env JWT demo key cũ | **Không phải lỗi** | Supabase local vẫn chấp nhận cả 2 key format (verified `curl /auth/v1/health` với cả 2 key → 200). .env JWT demo key vẫn hoạt động. |
| Image 333MB > 300MB lý tưởng | **Soft target** (không block) | NestJS prod deps nặng. M3 (build cả web) góp phần — fix sau. |
| GitHub Actions chạy thật | **Out of scope local** | Cần push + GitHub UI. Handoff leader. |
| Lighthouse chạy thật | **Out of scope local** | Cần web deploy production URL. Handoff leader. |
| **Lỗi story: KHÔNG có** (ngoài M1/M2/M3 từ review) | — | Tất cả test thật PASS. |

## 6. Note 3 MEDIUM từ review (M1/M2/M3)

| M | Mô tả | Trạng thái verify e2e | Fix |
|---|---|---|---|
| **M1** | `.lighthouserc.js` gate URL `/listings` không tồn tại trong web app | **CONFIRMED** — web app build output chỉ có route `/` + `/_not-found` (KHÔNG có `/listings`). Gate `/listings` sẽ 404 → spurious Lighthouse fail trên mọi production deploy. AC4 = PARTIAL. | **Fix sau** (task fix MEDIUM): xóa URL `/listings` khỏi `.lighthouserc.js` tới khi story 3.3 sinh SSR page `/listings`. Gate `/` là đủ cho 1.3. |
| **M2** | `deploy.yml` job `deploy-api` thiếu `permissions: packages: write` | **CONFIRMED** — đọc deploy.yml line 96-130: job `deploy-api` KHÔNG có `permissions:` block (chỉ `lighthouse-fail-issue` có `permissions: issues: write`). GHCR push có thể 403 nếu repo dùng restricted token permissions. | **Fix sau** (task fix MEDIUM): thêm `permissions: contents: read, packages: write` vào job `deploy-api`. |
| **M3** | `Dockerfile` builder chạy `yarn build` (build cả 3 package incl. web) thay vì chỉ api+shared | **CONFIRMED** — Dockerfile line 24: `RUN yarn build` = `turbo run build` → build shared + api + web. Runner stage chỉ copy `packages/shared/dist` + `apps/api/dist` (web dist bị bỏ). Docker build vẫn SUCCESS (web build không cần env bắt buộc). | **Fix sau** (task fix MEDIUM): đổi thành `yarn turbo build --filter=@bdsai/api...` (chỉ build shared + api). |

→ **Cả 3 MEDIUM confirmed tồn tại, non-blocking cho story pass. Fix ở task fix MEDIUM sau e2e.**

## 7. Teardown confirmation

| Step | Kết quả |
|---|---|
| `docker stop bdsai-smoke` | ✅ `bdsai-smoke` |
| `docker rm bdsai-smoke` | ✅ `bdsai-smoke` |
| `supabase stop` | ✅ "Stopped supabase local development setup." |
| `lsof -i :3101 -i :54351 -i :54352` | ✅ (rỗng — port free) |
| `rm -f /tmp/bdsai-*.log /tmp/smoke*.json` | ✅ (file tạm xóa) |
| `docker rmi bdsai-api:test` | ⏸ Giữ lại image (optional — leader/reviewer có thể verify lại) |

→ **Teardown sạch. Port free, container sạch, Supabase stop.**

## 8. Kết luận

**Verdict: PASS** — Story 1.3 CI/CD hạ tầng verified bằng REAL DATA:

1. ✅ **Workflow syntax** — actionlint 1.7.12 PASS (cài mới, không chỉ YAML parse)
2. ✅ **Docker build** — BUILD SUCCESS, multi-stage, 333MB
3. ✅ **Docker smoke test** — container boot OK, HTTP 200 `/health/supabase` với real Supabase (db:up, auth:up, storage:up), Docker HEALTHCHECK = healthy
4. ✅ **yarn build/lint/typecheck/test** — 3/3, 4/4, 4/4, 11/11 PASS (không phá 1.1/1.2)
5. ✅ **Secret audit** — grep clean (workflow + Dockerfile), docker history clean (no secret baked)
6. ✅ **Boundary-crossing** — Dockerfile khai báo khớp container runtime (EXPOSE 3101, HEALTHCHECK, CMD, ConfigService env)
7. ⚠️ **AC4 PARTIAL** — Lighthouse gate `/listings` broken (M1), gate `/` OK. Fix sau.

**3 MEDIUM (M1/M2/M3) confirmed, non-blocking — fix ở task fix MEDIUM sau e2e.**

**Handoff leader:**
- GitHub Actions chạy thật trên GitHub UI: push branch test → confirm (out of scope local).
- Lighthouse chạy thật trên production URL: cần web deploy (out of scope local).
- Cấu hình GitHub Secrets (SECRETS.md) + GHCR package settings trước production deploy đầu.
- Fix M1/M2/M3 trước production deploy đầu.
